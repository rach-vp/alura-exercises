const { gql } = require('apollo-server');

module.exports = gql`
  type User {
    id: ID!
    name: String!
    active: Boolean!
    email: String
    role: Role!
  }

  type Role {
    id: ID!,
    type: String!
  }

  type Query {
    users: [User!]!
    userById(id: ID!): User!
  }
`;
